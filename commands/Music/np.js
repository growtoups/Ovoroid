module.exports = ({
    name: "np",
	code: `$description[Currently playing: $songInfo[title], Requested by $username[$songInfo[userID]].

Duration: $songInfo[duration]

URL to Video: $songInfo[url]]`
})